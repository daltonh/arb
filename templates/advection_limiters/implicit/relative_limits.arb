# calculate the upwind cell-centred phi which will be used to identify relative limits
INCLUDE "upwind_phi"

# here we look at all surrounding cells to find bounds on phi
CELL_DERIVED <phi upper bound> "cellmax(<phi_c low order>,region=<cellicells>)" ON <domain>
CELL_DERIVED <phi lower bound> "cellmin(<phi_c low order>,region=<cellicells>)" ON <domain>
