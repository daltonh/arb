MARKDOWN
Small wrapper file for equations template file, which specifies the local width of a domain to be equal to <W_f> and <W_c>, for face and cell centres, respectively.  Hence requires that <W_f> and <W_c> are already defined (possibly as CONSTANT fields?)
END_MARKDOWN

INCLUDE "equations_nonuniform_width" R "<<W_f>>" W "<W_f>" R "<<W_c>>" W "<W_c>"
